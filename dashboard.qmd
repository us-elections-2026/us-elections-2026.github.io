---
title: "전망 대시보드"
subtitle: "2026 상원 — 경합주 판세·외부 예측·일정 (Cook·Sabato / 예측시장 / 외부 모델)"
include-in-header:
  - text: |
      <script src="https://cdn.jsdelivr.net/npm/chart.js@4.5.1"></script>
      <link rel="stylesheet" href="assets/dashboard.css">
---

```{r}
#| include: false
source(file.path("R", "helpers.R"))
```

::: {.callout-warning appearance="simple"}
이 페이지는 **성격이 다른 세 종류의 외부 예측**을 종류 구분해 종합합니다 — **등급**(Cook·Sabato: 확률이 아닌 서수적 판단), **예측시장 가격**(Kalshi: 확률의 근사일 뿐, 유동성·편향 혼입), **모델 확률**(RaceToTheWH·DDHQ: 각 기관 방법론의 산출물). 같은 잣대로 직접 비교하지 마세요. 시장 가격은 **270towin의 등급 환산값이 아니라 Kalshi 공개 API의 원가격**을 직접 받아 싣습니다(2026-08-06부터, 감시 8주 전부). 데이터를 갱신하려면 `data/model_dashboard.json`을 고쳐 push하면 사이트가 다시 빌드되어 자동 반영됩니다.
:::

::: {#dash-root}

<p id="dash-stamp" class="text-muted" style="font-size:.85rem;"></p>

### 핵심 지표
::: {#dash-kpis}
:::

### 민주 다수 확률 — 예측기관별 위치

한 축 위의 위치가 곧 이견의 크기입니다. **대표값은 예측시장 가격(45%)**입니다 — 이 페이지에서 원천까지 확인되는 유일한 상원 다수 수치이기 때문입니다(2026-08-06 승격). 함께 찍힌 RaceToTheWH·DDHQ는 7월 말 인용치로 **미검증** 상태이니, 축 위의 거리를 곧바로 "기관 간 이견"으로 읽지 마세요.

::: {#dash-scenarios}
:::

### 주별 민주 승리 전망 — 모델 막대 × 시장가 ◆

막대 = 자체 모델 v0(등급+시장+조사 블렌드), ◆ = 예측시장 가격(확인된 주만). 점선 = 50%.

<div style="height:420px; position:relative;"><canvas id="probChart"></canvas></div>

### 예측시장이 보는 상원 다수 {#market}

```{r}
#| output: asis
cat(kalshi_control_html())
```

시장은 민주당의 상원 다수 확률을 **45%**로 매깁니다. 이 값은 Kalshi 공개 API에서 원가격을 직접 받은 것이라 원천까지 확인됩니다. **근거는 아래 주별 가격에 있습니다** — 시장은 노스캐롤라이나·메인·알래스카·오하이오·텍사스를 등급기관보다 훨씬 민주 쪽으로 매깁니다. 민주당은 순 +4가 필요한데, 시장 가격대로면 NC·ME·AK·OH만으로 문턱에 닿습니다.

::: {.callout-important appearance="simple"}
**모델 쪽 수치는 재확인이 필요합니다 (2026-08-06)** — 이 페이지가 "외부 모델 13%"로 적어온 값은 7월 말 인용치이며, 원 사이트(RaceToTheWH)의 수치가 스크립트로 렌더돼 이번에 재확인하지 못했습니다. 함께 근거로 적혀 있던 "270toWin 컨센서스 ~87–88% 공화"는 **근거로 쓸 수 없습니다**: 270toWin의 "Which Party Will Control the Senate?"는 독립 모델이 아니라 **Kalshi 시장 위젯**이고(같은 페이지가 "most recent 'yes' trade"라고 밝히고 있습니다) 지금 공화 55%를 표시합니다. 따라서 "모델 13% 대 시장 45%"를 **이견으로 단정하지 마세요** — 한쪽이 검증되지 않은 상태입니다. RaceToTheWH는 2026 상원 모델을 실제로 운영하므로(일 5만 회 시뮬레이션), 원 수치가 확인되는 대로 갱신합니다.
:::

```{r}
gt_kalshi_races("senate", states = c("GA","NC","NH","MI","ME","AK","OH","TX"))
```

거래액을 함께 보세요. **텍사스($723만)·메인($370만)**은 이 사이클에서 가장 두꺼운 시장이고, 반대로 뉴햄프셔($16만)는 얇아 소수 거래에 흔들릴 수 있습니다. 가격이 반반에 가까운데 거래액이 큰 곳(텍사스·오하이오)이야말로 시장이 "정말 모르겠다"고 말하는 지점입니다.

### 경합주 등급 — Cook · Sabato 올해 변동 (3월 → 7월)

```{r}
#| output: asis
cat(rating_matrix_html())
```

### 현재 등급 — 9개 기관 비교

```{r}
gt_senate_rating_sources()
```

한 기관만 인용하면 판세가 단정적으로 보이지만, 아홉 곳을 나란히 놓으면 **어디가 진짜 불확실한지**가 드러납니다. 메인·미시간·알래스카는 대부분 토스업으로 일치하는 반면, **오하이오는 Toss-up(Cook·Sabato)에서 Tilt R(Inside Elections)·Lean R(Almanac)·Tilt D(RaceToTheWH)까지 벌어지고**, 텍사스도 Likely R(Inside Elections)에서 Toss-up(DDHQ·RtWH)까지 걸쳐 있습니다.

::: {.callout-note appearance="simple" collapse="true"}
## 표에 대한 두 가지 주석 (2026-08-06)

**`Tilt`가 새로 보이는 이유** — 270towin 피드는 등급을 코드로 압축해 싣는데, 그중 `a`·`b` 두 코드의 의미를 확인하지 못해 그동안 해당 칸을 `—`로 비워두고 있었습니다. 270towin 자체 지도 스크립트에서 정의를 확인해(`a`=Tilt D, `b`=Tilt R) 이제 값을 채웁니다. 하필 가장 접전인 칸들이 여기 걸려 있어, 메인·뉴햄프셔·오하이오·미시간·알래스카의 일부 기관 평가가 이번에 처음 표시됩니다. **Tilt는 Lean보다 좁은**(더 접전인) 등급으로 Cook·Sabato는 쓰지 않습니다.

**Split Ticket이 빠진 이유** — 8/5자 Split Ticket 지도는 2026년 선거가 없는 의석 23곳까지 색이 칠해져 있어, 이 사이트가 쓰는 좌표 규칙으로는 어느 값이 어느 레이스인지 확정할 수 없었습니다(그대로 읽으면 앨라배마가 민주 우세로 나옵니다). 추정으로 배정하지 않고 **해당 기관을 표에서 제외**했습니다. 원 피드가 통상적인 형식으로 돌아오면 자동으로 복귀합니다.
:::

### 경합주 상세
::: {#dash-states}
:::

### 향후 주요 일정
::: {#dash-timeline}
:::

<p id="dash-note" class="text-muted" style="font-size:.78rem; margin-top:1rem;"></p>

:::

<script src="assets/dashboard.js"></script>
