# 문체 규약 — 이 레포 전용

**적용 범위는 이 저장소(us-elections-2026.github.io)뿐이다.** 전역 스타일가이드
(`~/…/.claude/style-guide/`)는 학술 논문·보고서용이고 이 사이트에 적용하지 않는다.
반대로 이 규약을 다른 프로젝트로 가져가지도 않는다.

읽는 사람은 NYT·538·Silver Bulletin을 직접 볼 수 있는 층이다. 쉽게 쓰라는 뜻이 아니라,
**같은 정보를 더 적은 마찰로** 전달하라는 뜻이다.

## 1. 종결체 — 지면마다 고정

| 지면 | 종결체 | 이유 |
|---|---|---|
| 상시 페이지 (`*.qmd`, `states/`, `governors/`) | **습니다체** | 설명·안내 지면 |
| 주간호 (`issues/`) | **다체** | 분석·보도 지면 |

**한 파일 안에서 섞지 않는다.** 인용문·표 안의 원문은 예외.

## 2. 문장 — 100자에서 끊는다

- 목표: **100자 초과 문장이 전체의 10% 이하**
- 절을 셋 이상 물리지 않는다. "…하면서, …받아, …했습니다" 같은 사슬은 끊어 쓴다.
- 정보를 덧붙이고 싶으면 **다음 문장으로 넘긴다**. 괄호·줄표로 밀어 넣지 않는다.

## 3. 강조 — 아껴야 강조가 된다

- **굵게는 1,000자당 4개 이하.** 그 이상이면 눈이 굵은 글자를 무시하기 시작한다.
- 한 문단에 굵게가 셋 이상이면 무엇을 강조하려는지 스스로 되묻는다.
- 수치 자체는 굵게 하지 않는다 — **판단**을 굵게 한다.

## 4. 괄호·줄표 — 문장당 하나까지

출처·부연은 문장 끝으로 모으거나 다음 문장으로 뺀다.

## 5. 고유명사·용어 (기존 규칙 유지)

- 인명·기관명은 **영문 그대로**: Ossoff, Cook Political Report, Kalshi
- 주 이름은 한글 + 약자 병기: `조지아 (GA)`
- 마진은 항상 **양수 = 민주 우위**(`D+`)
- 등급·시장가·모델 확률은 성격이 달라 **같은 잣대로 비교하지 않는다**

## 6. 하지 말 것

- 수치·링크·인용을 문체 다듬기 중에 바꾸지 않는다. 문장 구조만 손댄다.
- **발행된 주간호(`issues/`)는 사후 수정하지 않는다.** 그 시점의 기록이다
  (2026-08-23 결정). 규약은 새로 쓰는 호부터 적용한다.

## 7. 확인

```bash
python3.11 scripts/check_prose.py          # 전체
python3.11 scripts/check_prose.py national.qmd
```
